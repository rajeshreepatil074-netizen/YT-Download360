[build]
base = "frontend"
# Ensure devDependencies (vite) are installed during Netlify builds
# Use npm ci for reproducible installs and force dev deps to be included
command = "NPM_CONFIG_PRODUCTION=false npm ci && npm run build"
publish = "dist"